// const login = () => {
//llamar api de login
// };

// const register = () => {};

// usar variables de ambiente .env, process.env."propiedad", para no repetir en cada servicio el localhost/api, solo se le pasa el path que corresponde.
// los nombres de las variables deben iniciar _app

//trainings

//users

// si usamos redux llamar los servicios que se requieran

// tener reducers, action types, etc.
